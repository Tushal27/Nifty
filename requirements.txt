numpy>=1.24
pandas>=2.0
pyyaml>=6.0
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
matplotlib>=3.7
yfinance>=0.2.40
pyarrow>=14.0
# Optional / heavy — only needed when the LSTM model is enabled in config.yaml
# tensorflow>=2.15
