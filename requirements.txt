numpy>=1.24
pandas>=2.0
pyyaml>=6.0
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
matplotlib>=3.7
yfinance>=0.2.40
requests>=2.28
pyarrow>=14.0
# Deep-learning (LSTM) model. Heavy (~hundreds of MB); install the CPU build.
# Required because models.lstm is enabled in config.yaml.
tensorflow-cpu>=2.15
